<% content_for :body_content_left do %>
  <ul id="companies">
    <% @companies.each do |company| %>
      <li>
        <%= link_to company.url, refinery.companies_company_path(company) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
