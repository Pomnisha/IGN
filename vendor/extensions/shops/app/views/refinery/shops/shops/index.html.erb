<% content_for :body_content_left do %>
  <ul id="shops">
    <% @shops.each do |shop| %>
      <li>
        <%= link_to shop.url, refinery.shops_shop_path(shop) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
